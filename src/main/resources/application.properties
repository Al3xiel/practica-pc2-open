# Spring Application Name
spring.application.name=si729pc2u202220033

# Spring Data Source Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/medelect
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Spring Data JPA Configuration
spring.jpa.show-sql=true

# Spring Data JPA Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true

# Spring Data JPA Hibernate Naming Strategy
spring.jpa.hibernate.naming.physical-strategy=pe.medelect.platform.u202220033.shared.domain.model.strategy.SnakeCaseWithPluralizedTablePhysicalNamingStrategy

# Server POrt Configuration
server.port=8095